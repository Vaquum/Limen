# Research runtime plus test tooling for pr_checks_tests, pr_checks_packaging
# (reproducible-build job), pr_checks_codeql, and the pr_publish_pypi SBOM env.
# Regenerate: uv pip compile requirements/ci/research-env.in --generate-hashes --no-emit-package vaquum-limen \
#   --python-version 3.10 --python-platform linux -o requirements/ci/research-env.txt
.[boosting,data,indicators,stats]
coverage[toml]>=7,<8
hypothesis>=6,<7
pytest>=8,<10
